# =============================================================================
# RSVP Reader TEST Environment Configuration (Example)
# =============================================================================
# Copy this file to .env.test and update the values as needed
# This file is safe to commit - it contains no secrets
#
# Quick Start:
#   1. cp .env.test.example .env.test
#   2. npx supabase start
#   3. Update READWISE_ACCESS_TOKEN in .env.test with a TEST account token
#   4. Run tests: npm test
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Local Development (Test Environment)
# -----------------------------------------------------------------------------
# These are the standard local development credentials
# They are the same for everyone running Supabase locally
# Generated by: npx supabase start
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
SUPABASE_SECRET_KEY=sb_secret_N7UND0UgjKTVK-Uodkm0Hg_xSvEMPvz

# Direct database connection (for migrations/testing)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# -----------------------------------------------------------------------------
# Readwise Reader API (TEST ACCOUNT ONLY!)
# -----------------------------------------------------------------------------
# ⚠️  WARNING: Create a SEPARATE Readwise account for testing!
# Do NOT use your production Readwise account for tests
# Tests will create, modify, and delete documents
#
# Steps:
#   1. Create a new Readwise account at https://readwise.io (use +test email trick)
#   2. Get API token from: https://readwise.io/access_token
#   3. Paste the token below
READWISE_ACCESS_TOKEN=your-test-readwise-token-here

# -----------------------------------------------------------------------------
# Sync API Protection
# -----------------------------------------------------------------------------
# For local development, a simple key is fine (not critical for security)
SYNC_API_KEY=local-dev-sync-key-not-for-production

# -----------------------------------------------------------------------------
# Optional: Rate Limiting Configuration
# -----------------------------------------------------------------------------
# Relaxed limits for local development
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# -----------------------------------------------------------------------------
# Node Environment
# -----------------------------------------------------------------------------
NODE_ENV=test

# -----------------------------------------------------------------------------
# Encryption Key (for sensitive data at rest)
# -----------------------------------------------------------------------------
# Used to encrypt sensitive tokens in the database
# Generate: openssl rand -base64 32
ENCRYPTION_KEY=your-test-encryption-key-here
